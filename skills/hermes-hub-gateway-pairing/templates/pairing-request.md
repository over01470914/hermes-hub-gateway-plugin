Use the `hermes-hub-gateway-pairing` skill for this request. Execute its local pairing wrapper exactly once and return only the wrapper's single output line.

Router: {{ROUTER_URL}}
Request ID: {{PAIRING_REQUEST_ID}}
Capabilities: {{CAPABILITIES}}
Expires at: {{EXPIRES_AT_ISO}}
